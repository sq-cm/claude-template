# CLAUDE fixture

@{QAComplianceReviewer} verifies against the declared locale at the QA Gate; a mismatch is a flag, not a block (unless in a compliance-sensitive claim).

Two commands: `/chat` and `/clear`.
