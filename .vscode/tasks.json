{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Install / Update Vault (macOS/Linux)",
      "type": "shell",
      "command": "./install.sh",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": [],
      "presentation": {
        "reveal": "always",
        "panel": "dedicated",
        "clear": true
      }
    },
    {
      "label": "Install / Update Vault (Windows)",
      "type": "shell",
      "command": "${workspaceFolder}\\install.bat",
      "windows": {
        "command": "${workspaceFolder}\\install.bat"
      },
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": [],
      "presentation": {
        "reveal": "always",
        "panel": "dedicated",
        "clear": true
      }
    }
  ]
}
