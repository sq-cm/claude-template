# Resources/Refs — Reference Index

_Use this index to find relevant doc refs for a task._

---

## Reference Index

> **Empty by design on fresh clones.** This index is populated by `/import-ref`. The FTS5 knowledge base (`ctx_search`) is machine-local and not synced — rows here are re-fetchable pointers (URL + fetch date), not vault-stored content; re-run `/import-ref` with the same URL on another machine to repopulate its local KB.

| Name | Description | Tags | Source URL | Fetch Date |
|------|-------------|------|-------------|------------|
